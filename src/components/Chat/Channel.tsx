import classes from './Chat.module.scss';

const { channelContainer } = classes;

const Channel = ({ }) => {
    return (
        <section className={channelContainer}>
            <h2>Channel</h2>
        </section>
    );
};

export default Channel;